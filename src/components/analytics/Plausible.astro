---

export interface Props {
  domain: string;
  src?: string;
}

const { domain, src = 'https://plausible.io/js/script.js' } = Astro.props;
---
<script src={src} data-domain={domain} is:inline defer></script>
